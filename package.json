{
  "name": "fullstack-app",
  "private": true,
  "scripts": {
    "setup": "npm install && cd frontend/device-management-ui && npm install",
    "start": "concurrently -n BACKEND,FRONTEND -c blue,green \"npm run backend\" \"npm run frontend\"",
    "backend": "dotnet run --launch-profile https --project backend/DeviceManagementSystem",
    "frontend": "cd frontend/device-management-ui && npm start"
  },
  "devDependencies": {
    "concurrently": "^8.0.0"
  }
}
